# Core dependencies
# (Currently ClaudeLang has minimal external dependencies)

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0

# Optional dependencies for development
black>=22.0.0
flake8>=4.0.0
isort>=5.0.0
mypy>=0.950

# Optional dependencies for specific features
# llvmlite>=0.39.0  # For LLVM backend
# numpy>=1.20.0     # For ML optimization features